# StoneVeld Studios Website

The public-facing StoneVeld Studios website.

## Local preview

Open `index.html` in a browser, or serve the directory with any static HTTP
server:

```bash
python -m http.server
```

Then visit <http://localhost:8000>.
